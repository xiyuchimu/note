指令区----线程独享
堆 
  --分代模型<1.8
     --新生代：8:1:1
     --老年代
     --永久代  
  --分代模型>=1.8
     --新生代：8:1:1
     --老年代
     --元空间(方法区)
     
方法区:动态加载.class文件。存储类信息、常量(1.7后有变化)、静态变量


指令区----线程独享
程序计数器：指向当前线程正在执行的字节码指令的地址（行号）
虚拟机栈:
  --FILO 先进后出
  --栈帧：一个方法一个栈帧
     --局部变量表:局部变量+引用
	 --操作数栈 :指令
	 --动态链接 ：指向字节码，用于多态加载类
	 --出口：return throw
本地方法栈
	 

	 
	

	
字节码一行是原子的
性能优化:吞吐率/最小执行时间
    --吞吐率:一个业务系统在单位时间内提供的产量